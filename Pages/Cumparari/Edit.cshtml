@page
@model MediiProgProiect.Pages.Cumparari.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Cumparare</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Cumparare.ID" />
            <div class="form-group">
                <label asp-for="Cumparare.MembruID" class="control-label"></label>
                <select asp-for="Cumparare.MembruID" class="form-control" asp-items="ViewBag.MembruID"></select>
                <span asp-validation-for="Cumparare.MembruID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Cumparare.MasinaID" class="control-label"></label>
                <input asp-for="Cumparare.MasinaID" class="form-control" />
                <span asp-validation-for="Cumparare.MasinaID" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Inapoi la lista</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
