@page
@model MediiProgProiect.Pages.Cumparari.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1></h1>

<p>
    <a asp-page="Create">Create Inregistrare Noua</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cumparare[0].Membru.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cumparare[0].MasinaC.Marca)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cumparare) {
        <tr>
            <td>
                    @Html.DisplayFor(modelItem => item.Membru.Email)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.MasinaC.Cumparare.MasinaC.Marca)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
